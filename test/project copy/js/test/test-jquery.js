$(function(){
  
  var project = $('<div />').appendTo('body').addClass('project'),
      headerProject = $('<header />').appendTo(project).addClass('project-name'),
      taskItem = '<div class=\'task-item\'>' +
                   '<i>' + '</i>' +
                 '</div>';
      
  taskItem.appendTo(project);
  
  for (var i=0; i<3; i ++) {
    taskItem;
  }               
      
      
 
  
  
});